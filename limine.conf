# Limine v11 config. Boot straight into oxbow; no menu.
timeout: 0

/oxbow
    protocol: limine
    path: boot():/boot/oxbow
    # The first user-mode server, delivered as a module the kernel loads.
    module_path: boot():/boot/server.elf
    module_cmdline: pong
